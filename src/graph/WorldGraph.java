package graph;

/**
 * Provides a graph structure for the specified portion of the world.
 * Each traversable square is treated as a vertex connected to its 4 neighboring (traversable) squares.
 * Edges are interpreted as connecting each square and are aware of its length and direction.
 */
public class WorldGraph {

}
